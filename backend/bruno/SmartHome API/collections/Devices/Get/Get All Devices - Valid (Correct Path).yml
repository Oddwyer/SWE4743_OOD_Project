info:
  name: Get All Devices - Valid (Correct Path)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5073/api/Devices
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
