info:
  name: Control Device - Set Thermostat Mode
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:5073/api/Devices/{deviceId}/commands
  body:
    type: json
    data: |-
      {
        "command": 4,
        "mode": "Auto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
