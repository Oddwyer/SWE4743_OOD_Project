info:
  name: Register Device - Invalid (FluentValidation Test)
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5073/api/Devices/register-device
  body:
    type: json
    data: |-
      {
        "deviceName": "",
        "deviceLocation": "",
        "type": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Tests FluentValidation rules by sending empty fields. 
  Expected: 400 Bad Request with validation errors.
