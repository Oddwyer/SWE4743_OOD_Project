info:
  name: Control Device - Set Color
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseUrl}}/api/Devices/0ae336e9-f22f-47b4-bb0f-868f69cfaecd/commands"
  body:
    type: json
    data: |-
      {
        "command": 2,
        "color": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that valid control commands are accepted and applied to a device.
