info:
  name: Get Device By ID - Invalid
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/Devices/45a5b095-901c-4e27-a1c9-62a18c9847d2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that requesting a non-existent device returns a 404 via middleware.
