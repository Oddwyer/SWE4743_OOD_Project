info:
  name: Location - Get Ambient Temp
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/locations/living room/ambient-temperature"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies retrieval of ambient temperature for a given location.
