info:
  name: Simulation - Speed (2x)
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/simulation/speed"
  body:
    type: json
    data: |-
      {
        "speedMultiplier": "twoX"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that unimplemented simulation functionality returns a 501 response via middleware.
