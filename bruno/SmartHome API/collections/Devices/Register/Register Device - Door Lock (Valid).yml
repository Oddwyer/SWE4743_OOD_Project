info:
  name: Register Device - Door Lock (Valid)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/Devices/"
  body:
    type: json
    data: |-
      {
        "deviceName": "Living Room Floor Lamp",
        "deviceLocation": "Living Room",
        "type": "Light"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that a device can be successfully registered with valid input data.
