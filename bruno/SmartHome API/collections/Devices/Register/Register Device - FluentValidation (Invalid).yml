info:
  name: Register Device - FluentValidation (Invalid)
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/Devices/"
  body:
    type: json
    data: |-
      {
        "deviceName": "",
        "deviceLocation": "",
        "type": "Light"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that invalid input (missing/incorrect fields) is rejected by FluentValidation.
