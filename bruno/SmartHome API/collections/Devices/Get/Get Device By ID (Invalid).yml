info:
  name: Get Device By ID (Invalid)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/Devices/73575364-74f3-4411-a300-fe401151e53f"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that requesting a non-existent device returns a 404 via middleware.
