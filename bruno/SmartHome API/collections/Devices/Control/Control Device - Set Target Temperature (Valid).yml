info:
  name: Control Device - Set Target Temperature (Valid)
  type: http
  seq: 11

http:
  method: PUT
  url: "{{baseUrl}}/api/Devices/f21cfc72-c982-44d0-8d6e-4706451d06b5/commands"
  body:
    type: json
    data: |-
      {
        "command": "setTargetTemperature",
        "targetTemperature": 68
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that valid control commands are accepted and applied to a device.
