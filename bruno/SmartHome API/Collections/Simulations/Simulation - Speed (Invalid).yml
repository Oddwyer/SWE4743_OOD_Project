info:
  name: Simulation - Speed (Invalid)
  type: http
  seq: 9

http:
  method: PUT
  url: "{{baseUrl}}/api/simulation/speed"
  body:
    type: json
    data: |-
      {
        "speedMultiplier": -1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies that invalid simulation speed values are rejected by validation.
