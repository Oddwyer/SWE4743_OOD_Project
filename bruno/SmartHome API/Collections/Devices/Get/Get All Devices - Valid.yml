info:
  name: Get All Devices - Valid
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/Devices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifies retrieval of all registered devices.
